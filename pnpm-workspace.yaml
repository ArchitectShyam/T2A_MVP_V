packages:
  - "apps/*"
  - "packages/*"

# Native deps allowed to run install scripts (pnpm >= 10 blocks these by default).
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
